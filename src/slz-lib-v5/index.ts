/**
 * Public entry point.
 *
 * `core` is the framework-agnostic engine and is exported on its own so it can
 * be consumed — and later packaged — without React (invariant 1).
 */
export * from "./core";
